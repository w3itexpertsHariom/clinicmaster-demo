{
  "name": "clinicmaster",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:site1 && npm run build:site2 && npm run build:site3",
    "build:site1": "cd site-1 && npm install && npm run build && npm run export && rm -rf ../public/site-1 && cp -r out ../public/site-1",
    "build:site2": "cd site-2 && npm install && npm run build && npm run export && rm -rf ../public/site-2 && cp -r out ../public/site-2",
    "build:site3": "cd site-3 && npm install && npm run build && npm run export && rm -rf ../public/site-3 && cp -r out ../public/site-3"
  }
}